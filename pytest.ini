[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --cov=compass
    --cov-report=term-missing
    --cov-report=html
    --no-cov-on-fail
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
